# Usado pelo docker-compose.yml para provisionar o Mongo com autenticacao.
# Copie para ".env" na raiz do projeto e defina uma senha forte - nunca commite o .env real.
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=
